services:
  - type: web
    name: poster-scraper-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m http.server $PORT & python main.py
    autoDeploy: true
